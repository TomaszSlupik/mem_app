import { createTheme } from '@mui/material/styles';

const themeColor = createTheme({
    palette: {
      primary: {
        main: "#9400d3",
      }, 
      secondary: {
        main: "#fff ",
      }
    },
  });
  
  export default themeColor